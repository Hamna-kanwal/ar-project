export const metadata = {
  title: "Landlord Gas Safety Certificate (CP12) Watford | AR Heating",
  description: "Landlord gas safety checks (CP12) in Watford. Under an hour, certificate the same day. Book today",
};

export default function BoilerLayout({ children }) {
  return <>{children}</>;
}